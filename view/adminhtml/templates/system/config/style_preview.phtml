<?php
/**
 * @var \MagoArab\WhatsappIcon\Block\Adminhtml\System\Config\StylePreview $block
 */
$styles = $block->getIconStyles();
$currentStyle = $block->getCurrentStyle();
?>
<div class="style-preview-wrapper">
    <div class="style-preview-header">
        <h3><?= __('WhatsApp Icon Style Preview') ?></h3>
        <p class="note"><?= __('Preview of available WhatsApp icon styles. Current selection is highlighted.') ?></p>
    </div>
    
    <div class="style-preview-grid">
        <?php foreach ($styles as $style): ?>
        <?php if (!empty($style['value'])): ?>
        <div class="style-preview-item <?= $style['value'] === $currentStyle ? 'current' : '' ?>">
            <div class="style-preview-image">
                <?php if ($block->hasStylePreview($style['value'])): ?>
                    <img src="<?= $block->getStylePreviewUrl($style['value']) ?>" 
                         alt="<?= $block->escapeHtmlAttr($style['label']) ?>"
                         title="<?= $block->escapeHtmlAttr($style['label']) ?>" />
                <?php else: ?>
                    <div class="no-preview">
                        <span class="icon-placeholder"><?= substr($style['label'], 0, 2) ?></span>
                    </div>
                <?php endif; ?>
            </div>
            <div class="style-preview-info">
                <h4><?= $block->escapeHtml($style['label']) ?></h4>
                <?php if ($style['value'] === $currentStyle): ?>
                    <span class="current-badge"><?= __('Current') ?></span>
                <?php endif; ?>
            </div>
        </div>
        <?php endif; ?>
        <?php endforeach; ?>
    </div>
    
    <div class="style-preview-note">
        <p><?= __('To change the icon style, use the "Icon Style" dropdown above.') ?></p>
        <p><?= __('The preview shows how the WhatsApp icon will appear on your website.') ?></p>
    </div>
</div>

<style>
.style-preview-wrapper {
    background: #fff;
    border: 1px solid #ddd;
    border-radius: 4px;
    padding: 20px;
    margin: 10px 0;
}

.style-preview-header h3 {
    margin: 0 0 5px 0;
    color: #333;
    font-size: 16px;
}

.style-preview-header .note {
    margin: 0 0 15px 0;
    color: #666;
    font-size: 13px;
}

.style-preview-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 15px;
    margin: 15px 0;
}

.style-preview-item {
    border: 2px solid #eee;
    border-radius: 8px;
    padding: 15px;
    text-align: center;
    transition: all 0.3s ease;
    background: #fafafa;
}

.style-preview-item:hover {
    border-color: #007cba;
    box-shadow: 0 2px 8px rgba(0, 124, 186, 0.1);
}

.style-preview-item.current {
    border-color: #007cba;
    background: #f0f8ff;
    box-shadow: 0 2px 8px rgba(0, 124, 186, 0.2);
}

.style-preview-image {
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 10px;
}

.style-preview-image img {
    max-width: 60px;
    max-height: 60px;
    border-radius: 50%;
}

.no-preview {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, #25d366, #128c7e);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto;
}

.icon-placeholder {
    color: white;
    font-weight: bold;
    font-size: 18px;
    text-transform: uppercase;
}

.style-preview-info h4 {
    margin: 0 0 5px 0;
    color: #333;
    font-size: 14px;
    font-weight: 600;
}

.current-badge {
    background: #007cba;
    color: white;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: bold;
    text-transform: uppercase;
}

.style-preview-note {
    margin-top: 15px;
    padding-top: 15px;
    border-top: 1px solid #eee;
}

.style-preview-note p {
    margin: 5px 0;
    color: #666;
    font-size: 12px;
}
</style>